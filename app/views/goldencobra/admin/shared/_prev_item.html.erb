<% table_name = resource.class.table_name %>
<% resource_to_edit = resource.class.where("#{table_name}.id < ?", resource.id).order("#{table_name}.id DESC").first %>
<% if resource_to_edit %>
  <%= link_to "<- #{active_admin_config.resource_label}", edit_resource_path(resource_to_edit) %>
<% end %>