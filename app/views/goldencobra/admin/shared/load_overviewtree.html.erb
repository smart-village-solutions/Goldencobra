<%# Es entstehen HTML requests auf die Ajax Funktion, sodass Fehler geschmissen werden. %>
<%# ActionView::MissingTemplate: Missing template /goldencobra/admin/shared/load_overviewtree with {:locale=>[:de], :formats=>[:html], :handlers=>[:erb, :builder, :slim, :arb, :csvbuilder]}. %>

<%# Grund: ungewiss %>
<%# Lösung: HTML-Datei anlegen, damit Request nicht ins Leere läuft %>
