language: ruby
before_script:
  - "mysql -e 'create database goldencobra_test;'"
bundler_args: --without development
before_script:
  #- "bundle exec gem install activerecord-mysql2-adapter"
  - "bundle exec rails g goldencobra:install"
  - "bundle exec rake db:setup db:test:prepare"
  - "bundle exec rake"
rvm:
  - 1.9.2
gemfile:
  - Gemfile
branches:
  only:
    - master
mysql:
  adapter: mysql2
  database: goldencobra_test
  username: root
  encoding: utf8
